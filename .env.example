# Set to false to use the real on-device Gemma 4 E2B model instead of the mock client.
# Requires a native build (npx expo prebuild + expo run:android) and a physical arm64
# Android device — see plan.md Phase 14 for the current bring-up status.
EXPO_PUBLIC_USE_MOCK_MODEL=true
